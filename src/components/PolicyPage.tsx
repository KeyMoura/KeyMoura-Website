import Link from "next/link";

export function PolicyPage({ eyebrow, title, updated, children }:{ eyebrow:string; title:string; updated:string; children:React.ReactNode }) {
  return <main className="mx-auto max-w-4xl px-4 py-12 sm:py-16"><header className="rounded-3xl border border-zinc-800 bg-[linear-gradient(145deg,rgba(24,24,27,.95),rgba(0,0,0,.75))] p-6 sm:p-10"><p className="text-xs font-semibold uppercase tracking-[.2em] text-brand-primary">{eyebrow}</p><h1 className="mt-3 text-3xl font-semibold sm:text-5xl">{title}</h1><p className="mt-3 text-sm text-brand-textMuted">Last updated {updated}</p></header><article className="policy-copy mt-8 space-y-8 rounded-3xl border border-zinc-800 bg-black/25 p-6 leading-7 text-brand-textMuted sm:p-10">{children}<section><h2 className="text-xl font-semibold text-brand-text">Questions?</h2><p className="mt-2">Contact <a className="text-brand-primary underline" href="mailto:support@keymoura.com">support@keymoura.com</a> before ordering if anything is unclear.</p></section></article><p className="mt-6 text-sm text-brand-textMuted"><Link className="underline hover:text-brand-primary" href="/catalog">Return to catalog</Link></p></main>;
}
